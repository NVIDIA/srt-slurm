[build-system]
requires = ["maturin>=1.10,<2"]
build-backend = "maturin"

[project]
name = "ruter"
version = "0.1.0"
description = "Post-hoc router benchmark observability"
requires-python = ">=3.10"

[tool.maturin]
bindings = "bin"
manifest-path = "Cargo.toml"
