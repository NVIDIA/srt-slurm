name: Release

# pull_request_target runs in the base repository context, which supplies the
# write-capable token that creates the release. Build jobs check out only the
# merged main-branch commit, after GitHub reports the pull request as merged.
on:
  pull_request_target:
    types: [closed]
    branches: [main]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  version:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.ver.outputs.tag }}
      target: ${{ github.event.pull_request.merge_commit_sha }}
      tachometer_changed: ${{ steps.changed.outputs.tachometer }}
      cpu_power_exporter_changed: ${{ steps.changed.outputs.cpu_power_exporter }}
    steps:
      - name: Detect changed binaries
        id: changed
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail
          files=$(gh api --paginate "repos/${GH_REPO}/pulls/${PR_NUMBER}/files?per_page=100" --jq '.[].filename')

          if printf '%s\n' "$files" | grep -Eq '^(Cargo\.(toml|lock)|rust-toolchain\.toml|src/tachometer/|docker/Dockerfile\.tachometer-scraper$|\.dockerignore$|\.github/workflows/release\.yaml$)'; then
            tachometer=true
          else
            tachometer=false
          fi
          echo "Tachometer changed: $tachometer"
          echo "tachometer=$tachometer" >> "$GITHUB_OUTPUT"

          if printf '%s\n' "$files" | grep -Eq '^(Cargo\.(toml|lock)|rust-toolchain\.toml|src/cpu-power-exporter/|docker/Dockerfile\.cpu-power-exporter$|\.dockerignore$|\.github/workflows/release\.yaml$)'; then
            cpu_power_exporter=true
          else
            cpu_power_exporter=false
          fi
          echo "cpu-power-exporter changed: $cpu_power_exporter"
          echo "cpu_power_exporter=$cpu_power_exporter" >> "$GITHUB_OUTPUT"

      - name: Compute next version
        id: ver
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          # "true" when the merged PR carries the new-feature label -> minor bump.
          IS_FEATURE: ${{ contains(github.event.pull_request.labels.*.name, 'new-feature') }}
        run: |
          set -euo pipefail
          # Bump from the HIGHEST existing GitHub Release (independent of the
          # in-code version). A PR labeled "new-feature" bumps the MINOR and
          # resets patch (v1.0.5 -> v1.1.0); otherwise the PATCH bumps
          # (v1.0.5 -> v1.0.6). MAJOR is never bumped automatically -- publish a
          # higher major by hand and the next merge continues from there.
          # First run only (no release yet): seed v1.0.0.
          latest=$(gh release list --limit 1000 --json tagName --jq '.[].tagName' | sort -V | tail -n1)
          if [ -z "$latest" ]; then
            next="v1.0.0"
          else
            core=${latest#v}; core=${core%%.post*}; core=${core%%-*}
            IFS='.' read -r major minor patch <<< "$core"
            if [ "$IS_FEATURE" = "true" ]; then
              next="v${major}.$((minor + 1)).0"
            else
              next="v${major}.${minor}.$((patch + 1))"
            fi
          fi
          echo "Latest release: ${latest:-none}  new-feature: $IS_FEATURE  Next: $next"
          echo "tag=$next" >> "$GITHUB_OUTPUT"

  build-tachometer-scraper:
    needs: version
    if: needs.version.outputs.tachometer_changed == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            asset: tachometer-scraper-x86_64-unknown-linux-gnu
          - platform: linux/arm64
            asset: tachometer-scraper-aarch64-unknown-linux-gnu
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.version.outputs.target }}

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - name: Build scraper binary
        run: |
          set -euo pipefail
          docker buildx build \
            --platform "${{ matrix.platform }}" \
            --file docker/Dockerfile.tachometer-scraper \
            --target artifact \
            --output type=local,dest=build/tachometer \
            .
          mkdir -p dist
          mv build/tachometer/tachometer-scraper "dist/${{ matrix.asset }}"
          chmod +x "dist/${{ matrix.asset }}"
          (cd dist && sha256sum "${{ matrix.asset }}" > "${{ matrix.asset }}.sha256")

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: dist/
          retention-days: 7

  build-cpu-power-exporter:
    needs: version
    if: needs.version.outputs.cpu_power_exporter_changed == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            asset: cpu-power-exporter-x86_64-unknown-linux-musl
          - platform: linux/arm64
            asset: cpu-power-exporter-aarch64-unknown-linux-musl
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.version.outputs.target }}

      # No QEMU: the Dockerfile builds on $BUILDPLATFORM and cross-links, and
      # the artifact stage only copies the result.
      - uses: docker/setup-buildx-action@v3

      - name: Build cpu-power-exporter binary
        run: |
          set -euo pipefail
          docker buildx build \
            --platform "${{ matrix.platform }}" \
            --file docker/Dockerfile.cpu-power-exporter \
            --target artifact \
            --output type=local,dest=build/cpu-power-exporter \
            .
          mkdir -p dist
          mv build/cpu-power-exporter/cpu-power-exporter "dist/${{ matrix.asset }}"
          chmod +x "dist/${{ matrix.asset }}"
          (cd dist && sha256sum "${{ matrix.asset }}" > "${{ matrix.asset }}.sha256")

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: dist/
          retention-days: 7

  release:
    needs: [version, build-tachometer-scraper, build-cpu-power-exporter]
    if: >-
      always() &&
      needs.version.result == 'success' &&
      (needs.build-tachometer-scraper.result == 'success' || needs.build-tachometer-scraper.result == 'skipped') &&
      (needs.build-cpu-power-exporter.result == 'success' || needs.build-cpu-power-exporter.result == 'skipped')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        if: needs.version.outputs.tachometer_changed == 'true'
        with:
          pattern: tachometer-scraper-*
          path: dist
          merge-multiple: true

      - uses: actions/download-artifact@v4
        if: needs.version.outputs.cpu_power_exporter_changed == 'true'
        with:
          pattern: cpu-power-exporter-*
          path: dist
          merge-multiple: true

      - name: Carry forward unchanged binaries from the previous release
        if: needs.version.outputs.tachometer_changed != 'true' || needs.version.outputs.cpu_power_exporter_changed != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TACHOMETER_CHANGED: ${{ needs.version.outputs.tachometer_changed }}
          CPU_POWER_EXPORTER_CHANGED: ${{ needs.version.outputs.cpu_power_exporter_changed }}
        run: |
          set -euo pipefail
          previous=$(gh release list --limit 1000 --json tagName --jq '.[].tagName' | sort -V | tail -n1)
          mkdir -p dist
          # Every merge cuts a release, so an unchanged binary is re-uploaded
          # from the previous one to keep each release self-contained. The
          # expected set is both architectures plus their checksums. A
          # previous release holding only part of that set is damaged, not a
          # gap: carrying the remainder forward would quietly propagate a
          # release that `make setup` cannot install on one architecture. Only
          # a previous release with none of the assets is a real gap (the
          # first-ever release, or a binary introduced after that tag).
          carry() {
            prefix=$1
            shift
            expected=""
            for suffix in "$@"; do
              expected="$expected $prefix-$suffix $prefix-$suffix.sha256"
            done
            if [ -z "$previous" ]; then
              echo "::warning::No previous release to carry $prefix forward from; this release omits it"
              return 0
            fi
            if ! assets=$(gh release view "$previous" --json assets --jq '.assets[].name'); then
              echo "::error::Could not list assets of previous release $previous"
              return 1
            fi
            found=0
            missing=""
            for name in $expected; do
              if printf '%s\n' "$assets" | grep -qxF "$name"; then
                found=$((found + 1))
              else
                missing="$missing $name"
              fi
            done
            if [ "$found" -eq 0 ]; then
              echo "::warning::Previous release $previous has no $prefix assets; this release omits them"
              return 0
            fi
            if [ -n "$missing" ]; then
              echo "::error::Previous release $previous is missing $prefix assets:$missing"
              return 1
            fi
            if ! gh release download "$previous" --pattern "$prefix-*" --dir dist; then
              echo "::error::Failed to download $prefix assets from $previous"
              return 1
            fi
            for name in $expected; do
              if [ ! -f "dist/$name" ]; then
                echo "::error::Carried-forward $name never landed in dist"
                return 1
              fi
            done
          }
          [ "$TACHOMETER_CHANGED" = "true" ] || carry tachometer-scraper x86_64-unknown-linux-gnu aarch64-unknown-linux-gnu
          [ "$CPU_POWER_EXPORTER_CHANGED" = "true" ] || carry cpu-power-exporter x86_64-unknown-linux-musl aarch64-unknown-linux-musl

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.version.outputs.tag }}
          TARGET: ${{ needs.version.outputs.target }}
        run: |
          set -euo pipefail
          shopt -s nullglob
          # An unexpanded dist/* glob would be passed through as a literal path.
          assets=(dist/*)
          # Each binary is either absent from this release entirely (nothing
          # built and nothing to carry forward) or complete for both
          # architectures with checksums. Half a set installs on one
          # architecture and 404s on the other.
          for spec in "tachometer-scraper x86_64-unknown-linux-gnu aarch64-unknown-linux-gnu" \
                      "cpu-power-exporter x86_64-unknown-linux-musl aarch64-unknown-linux-musl"; do
            set -- $spec
            prefix=$1
            shift
            present=()
            missing=()
            for suffix in "$@"; do
              for name in "$prefix-$suffix" "$prefix-$suffix.sha256"; do
                if [ -f "dist/$name" ]; then present+=("$name"); else missing+=("$name"); fi
              done
            done
            if [ "${#present[@]}" -ne 0 ] && [ "${#missing[@]}" -ne 0 ]; then
              echo "::error::$TAG would ship an incomplete $prefix set; missing: ${missing[*]}"
              exit 1
            fi
          done
          echo "Publishing $TAG with ${#assets[@]} asset(s)"
          gh release create "$TAG" "${assets[@]}" \
            --target "$TARGET" \
            --title "$TAG" \
            --generate-notes
